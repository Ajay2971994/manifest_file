apiVersion: v1
kind: Service
metadata:
  name:  nginx-service
  labels:
    app.kubernetes.io/name: nginx
spec:
  type: LoadBalancer
  selector:
    app:  nginx
  ports:
  - port:  80
    targetPort:  80